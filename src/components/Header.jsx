import React from "react";

const Header = () => {
  return (
    <header>
        <img src="" alt="logo" />
      <nav>
        <ul>
            {/* {
                navLinks.map((el) => (
                    <li key={el.id}><a href={`#${el.ref}`}>{el.title}</a></li>
                ));
            } */}
            
            
        </ul>
      </nav>
    </header>
  );
};

export default Header;
